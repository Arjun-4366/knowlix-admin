import HRReportsManager from "@/components/hr/reports/HRReportsManager";

export const metadata = {
  title: "HR Analytics & Reports | Knowlix HR",
  description: "Attendance summaries, payroll data, performance scores, and workforce turnover metrics.",
};

export default function HRReportsPage() {
  return <HRReportsManager />;
}
